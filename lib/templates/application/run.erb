#!/bin/sh
<%#
bash script to run exported application on linux
%>
APPDIR=$(dirname "$0")
export EXPORTED="true"
cd "$APPDIR/lib"
java -cp "<%= @linux_class_path %>" org.jruby.Main ruby-processing/runners/run.rb <%= @main_file %>
